// Select all elements individually
//const elements = [
  //...document.querySelectorAll('.home-screen-info-site-name'),
  //...document.querySelectorAll('.home-screen-info-links'),
  //...document.querySelectorAll('.home-screen-info-other'),
  //...document.querySelectorAll('.home-screen-info-another')
//];

//const fadeDistance = 200;

//window.addEventListener('scroll', () => {
  //const scrollY = window.scrollY;

  //elements.forEach(el => {
    //const rect = el.getBoundingClientRect();
    //const elementTop = rect.top + scrollY;

    // Fade out when near top
    //let opacity = 1;
    //if (scrollY + fadeDistance > elementTop) {
      //opacity = Math.max(0, 1 - ((scrollY + fadeDistance - elementTop) / fadeDistance));
    //}

    //el.style.opacity = opacity;
    //el.style.transform = `translateY(${(1 - opacity) * 30}px)`; // slide from bottom
    //el.style.transition = 'opacity 0.3s ease, transform 0.3s ease';
  //});
//});
